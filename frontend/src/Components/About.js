const About = () => {
  return (
    <div>
      The software will analyze the number of credits, the courses taken by each
      student and the remaining courses for their graduation. Then it will
      calculate the number of sections of each course that should be opened in
      the next semester, so the Chairperson would be able to improve their
      decision in this manner.
    </div>
  );
};

export default About;
